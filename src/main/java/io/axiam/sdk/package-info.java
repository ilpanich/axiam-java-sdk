/**
 * The AXIAM Java SDK — official client library for the AXIAM IAM system.
 *
 * <p>This SDK conforms to CONTRACT.md &sect;1&ndash;&sect;10.
 *
 * <p>The entire {@code io.axiam.sdk} package tree is null-marked by default
 * (D-05): every type, field, parameter, and return value is non-null unless
 * explicitly annotated {@link org.jspecify.annotations.Nullable @Nullable}.
 */
@NullMarked
package io.axiam.sdk;

import org.jspecify.annotations.NullMarked;
