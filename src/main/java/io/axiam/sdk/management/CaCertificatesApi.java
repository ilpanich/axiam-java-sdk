// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management;

import com.fasterxml.jackson.databind.JsonNode;
import io.axiam.sdk.internal.ManagementTransport;
import org.jspecify.annotations.Nullable;

import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;

/**
 * The ca_certificates management namespace (CONTRACT §27.1).
 *
 * <p>Organization CAs and the per-tenant signing CAs chained beneath them.
 *
 * <p>Acquiring a handle performs no I/O and allocates nothing meaningful (§27.2 rule 1); it holds
 * the transport and cannot be constructed without one.
 *
 * <p>Every operation here is synchronous. The SDK's CompletableFuture twins exist only on the §1
 * core operations, so the async form of a management call is CompletableFuture.supplyAsync(()
 * -&gt; client.management().caCertificates()...) — one line, rather than 146 more methods to keep
 * in step.
 */
public final class CaCertificatesApi {

    private final ManagementTransport transport;
    private final NamespaceScope scope;

    CaCertificatesApi(ManagementTransport transport, NamespaceScope scope) {
        this.transport = transport;
        this.scope = scope;
    }

    /**
     * Addresses a different organization than the client's own.
     *
     * <p>§27.4 rule 3: the client's organization is the default, and a platform-admin token
     * legitimately overrides it.
     *
     * @param orgId the organization to address
     * @return a new handle; this one is unchanged
     */
    public CaCertificatesApi inOrg(UUID orgId) {
        return new CaCertificatesApi(transport, scope.withOrg(orgId));
    }

    /**
     * Issues GET /api/v1/organizations/{org_id}/ca-certificates.
     *
     * @param page where the page starts and how big it is, or null for the server's default
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public Page<io.axiam.sdk.management.models.CaCertificate> list(@Nullable PageRequest page) {
        final String operation = "ca_certificates.list";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        ManagementSupport.pageQuery(query, page);
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/organizations/{org_id}/ca-certificates", "/api/v1/organizations/" + orgId + "/ca-certificates", query, null);
        return ManagementSupport.convertPage(node, io.axiam.sdk.management.models.CaCertificate.class, operation);
    }

    /**
     * Walks ca_certificates.list to exhaustion, concatenating every page.
     *
     * <p>The auto-paging form §27.4 rule 4 requires. It stops on an empty page even if the total
     * disagrees, so a misreporting server costs one wasted request rather than an unbounded loop.
     *
     * @param start where the walk begins, or null to start at the top
     * @return every item in the set, across as many requests as it takes
     */
    public java.util.List<io.axiam.sdk.management.models.CaCertificate> listAll(@Nullable PageRequest start) {
        return ManagementSupport.collectPages(start, p -> list(p));
    }

    /**
     * Issues POST /api/v1/organizations/{org_id}/ca-certificates.
     *
     * <p>Returns secret material, once. private_key_pem is returned by this call and by no other;
     * no later get will return it again, and the get projection has no field where it was.
     * Discarding the result destroys the credential (§27.5 rule 3).
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.GeneratedCaCertificate generate(io.axiam.sdk.management.models.CreateCaCertificateRequest body) {
        final String operation = "ca_certificates.generate";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "POST",
                "/api/v1/organizations/{org_id}/ca-certificates", "/api/v1/organizations/" + orgId + "/ca-certificates", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.GeneratedCaCertificate.class, operation);
    }

    /**
     * Issues POST /api/v1/organizations/{org_id}/ca-certificates/import.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.CaCertificate importCa(io.axiam.sdk.management.models.ImportCaCertificateRequest body) {
        final String operation = "ca_certificates.import_ca";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "POST",
                "/api/v1/organizations/{org_id}/ca-certificates/import", "/api/v1/organizations/" + orgId + "/ca-certificates/import", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.CaCertificate.class, operation);
    }

    /**
     * Issues GET /api/v1/organizations/{org_id}/ca-certificates/{id}.
     *
     * @param id the id path parameter
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.CaCertificate get(java.util.UUID id) {
        final String operation = "ca_certificates.get";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/organizations/{org_id}/ca-certificates/{id}", "/api/v1/organizations/" + orgId + "/ca-certificates/" + id + "", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.CaCertificate.class, operation);
    }

    /**
     * Issues POST /api/v1/organizations/{org_id}/ca-certificates/{id}/revoke.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param id the id path parameter
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void revoke(java.util.UUID id) {
        final String operation = "ca_certificates.revoke";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        transport.send(operation, "POST",
                "/api/v1/organizations/{org_id}/ca-certificates/{id}/revoke", "/api/v1/organizations/" + orgId + "/ca-certificates/" + id + "/revoke", query, null);
    }

    /**
     * Issues POST /api/v1/organizations/{org_id}/ca-certificates/{id}/migrate-custody.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param id the id path parameter
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.MigrateCustodyResponse migrateCustody(java.util.UUID id) {
        final String operation = "ca_certificates.migrate_custody";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "POST",
                "/api/v1/organizations/{org_id}/ca-certificates/{id}/migrate-custody", "/api/v1/organizations/" + orgId + "/ca-certificates/" + id + "/migrate-custody", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.MigrateCustodyResponse.class, operation);
    }

    /**
     * Issues PUT /api/v1/organizations/{org_id}/ca-certificates/{id}/mtls-trust-anchor.
     *
     * <p>This is a REPLACEMENT, not a patch (§27.4 rule 5). Every field of the body is required,
     * and what you do not carry over from a prior read is not preserved -- it is overwritten. Read
     * first, change the field you mean, send the whole thing back.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param id the id path parameter
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.MtlsTrustAnchorResponse setMtlsTrustAnchor(java.util.UUID id, io.axiam.sdk.management.models.SetMtlsTrustAnchor body) {
        final String operation = "ca_certificates.set_mtls_trust_anchor";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "PUT",
                "/api/v1/organizations/{org_id}/ca-certificates/{id}/mtls-trust-anchor", "/api/v1/organizations/" + orgId + "/ca-certificates/" + id + "/mtls-trust-anchor", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.MtlsTrustAnchorResponse.class, operation);
    }

    /**
     * Issues GET /api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas.
     *
     * @param tenantId the tenant_id path parameter
     * @param page where the page starts and how big it is, or null for the server's default
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public Page<io.axiam.sdk.management.models.CaCertificate> listSigningCas(java.util.UUID tenantId, @Nullable PageRequest page) {
        final String operation = "ca_certificates.list_signing_cas";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        ManagementSupport.pageQuery(query, page);
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas", "/api/v1/organizations/" + orgId + "/tenants/" + tenantId + "/signing-cas", query, null);
        return ManagementSupport.convertPage(node, io.axiam.sdk.management.models.CaCertificate.class, operation);
    }

    /**
     * Walks ca_certificates.list_signing_cas to exhaustion, concatenating every page.
     *
     * <p>The auto-paging form §27.4 rule 4 requires. It stops on an empty page even if the total
     * disagrees, so a misreporting server costs one wasted request rather than an unbounded loop.
     *
     * @param tenantId the tenant_id path parameter
     * @param start where the walk begins, or null to start at the top
     * @return every item in the set, across as many requests as it takes
     */
    public java.util.List<io.axiam.sdk.management.models.CaCertificate> listSigningCasAll(java.util.UUID tenantId, @Nullable PageRequest start) {
        return ManagementSupport.collectPages(start, p -> listSigningCas(tenantId, p));
    }

    /**
     * Issues POST /api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas.
     *
     * <p>Returns secret material, once. private_key_pem is returned by this call and by no other;
     * no later get will return it again, and the get projection has no field where it was.
     * Discarding the result destroys the credential (§27.5 rule 3).
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param tenantId the tenant_id path parameter
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.GeneratedCaCertificate generateSigningCa(java.util.UUID tenantId, io.axiam.sdk.management.models.CreateIntermediateCaRequest body) {
        final String operation = "ca_certificates.generate_signing_ca";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "POST",
                "/api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas", "/api/v1/organizations/" + orgId + "/tenants/" + tenantId + "/signing-cas", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.GeneratedCaCertificate.class, operation);
    }

    /**
     * Issues POST /api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas/sign-csr.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param tenantId the tenant_id path parameter
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.CaCertificate signSigningCaCsr(java.util.UUID tenantId, io.axiam.sdk.management.models.SignIntermediateCsrRequest body) {
        final String operation = "ca_certificates.sign_signing_ca_csr";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "POST",
                "/api/v1/organizations/{org_id}/tenants/{tenant_id}/signing-cas/sign-csr", "/api/v1/organizations/" + orgId + "/tenants/" + tenantId + "/signing-cas/sign-csr", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.CaCertificate.class, operation);
    }

}
