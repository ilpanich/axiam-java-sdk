// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;

import java.util.List;
import java.util.UUID;

/**
 * A permission grant with its scopes resolved.
 *
 * <p>A superset of [{@code PermissionGrant}]: {@code scope_ids} is still present and still
 * authoritative, so a client written before {@code scopes} existed is unaffected.
 *
 * @param effect Whether the grant permits or refuses.
 * @param permission The permission this grant is about.
 * @param scopeIds The scope ids the grant is constrained to. Empty is the wildcard.
 * @param scopes The scopes {@code scope_ids} names, in the same order where resolvable. A scope
 *     that cannot be resolved is **omitted** rather than represented by a placeholder, so this can be
 *     shorter than {@code scope_ids}. That happens when a scope was deleted while a grant still
 *     referenced it, and showing a name for something that no longer exists would be worse than
 *     showing one fewer chip beside a count that still says how many ids the grant carries.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record ResolvedPermissionGrant(
        @JsonProperty("effect") PermissionEffect effect,
        @JsonProperty("permission") Permission permission,
        @JsonProperty("scope_ids") List<UUID> scopeIds,
        @JsonProperty("scopes") List<GrantedScope> scopes
) {
}
