// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

import java.util.List;
import java.util.UUID;

/**
 * The GrantPermissionRequest schema from the server's OpenAPI document.
 *
 * @param effect B1: {@code "allow"} (the default) or {@code "deny"}. A deny grant **overrides
 *     every allow**, at any depth of the resource hierarchy and at equal specificity — it is not
 *     most-specific-wins. See {@code claude_dev/deny-override-design.md} for the precedence table and
 *     for why that trade was made. Omitting the field means {@code "allow"}, so every existing client
 *     keeps working unchanged and no migration is required.
 * @param permissionId the server's permission_id field
 * @param scopeIds the server's scope_ids field
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record GrantPermissionRequest(
        @JsonProperty("effect") @Nullable PermissionEffect effect,
        @JsonProperty("permission_id") UUID permissionId,
        @JsonProperty("scope_ids") @Nullable List<UUID> scopeIds
) {
}
