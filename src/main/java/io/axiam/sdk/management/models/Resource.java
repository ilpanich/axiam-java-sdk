// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

import com.fasterxml.jackson.databind.JsonNode;
import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * The Resource schema from the server's OpenAPI document.
 *
 * @param createdAt the server's created_at field
 * @param id the server's id field
 * @param metadata the server's metadata field
 * @param name the server's name field
 * @param parentId Parent resource ID for hierarchical organization. {@code None} for root
 *     resources.
 * @param resourceType The type of resource (e.g., {@code project}, {@code service}, {@code
 *     endpoint}).
 * @param tenantId the server's tenant_id field
 * @param umaRegisteredBy The {@code client_id} that registered this resource through the UMA
 *     Protection API (X2), or {@code None} for a resource created any other way. Read-only from every
 *     ordinary path: [{@code UpdateResource}] cannot set it and [{@code CreateResource}] cannot
 *     either, so the only writer is the resource-registration handler. That is deliberate — the field
 *     backs a provenance badge in the admin UI, and a provenance marker anyone can write is decoration
 *     that reads like evidence.
 * @param updatedAt the server's updated_at field
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record Resource(
        @JsonProperty("created_at") OffsetDateTime createdAt,
        @JsonProperty("id") UUID id,
        @JsonProperty("metadata") JsonNode metadata,
        @JsonProperty("name") String name,
        @JsonProperty("parent_id") @Nullable UUID parentId,
        @JsonProperty("resource_type") String resourceType,
        @JsonProperty("tenant_id") UUID tenantId,
        @JsonProperty("uma_registered_by") @Nullable String umaRegisteredBy,
        @JsonProperty("updated_at") OffsetDateTime updatedAt
) {
}
