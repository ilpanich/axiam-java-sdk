// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

import io.axiam.sdk.Sensitive;

/**
 * Body of {@code POST /api/v1/organizations/{org_id}/ca-certificates/import}.
 *
 * <p>Deliberately carries no subject, validity window or key algorithm: all three are read out of
 * the certificate itself. A caller that could name them separately could name a subject the
 * certificate does not have, and AXIAM would enforce the claim while every relying party read the
 * certificate.
 *
 * @param privateKeyPem PEM-encoded private key, if AXIAM is to take custody of it. Omit it to
 *     register the certificate as a trust anchor only. Write-only: it goes to the configured custodian
 *     on the way in and no endpoint returns it. -- SECRET: redacted from toString and from every JSON
 *     rendering except the one request body it is sent in
 * @param publicCertPem PEM-encoded CA certificate.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record ImportCaCertificateRequest(
        @JsonProperty("private_key_pem") @Nullable Sensitive privateKeyPem,
        @JsonProperty("public_cert_pem") String publicCertPem
) {
}
