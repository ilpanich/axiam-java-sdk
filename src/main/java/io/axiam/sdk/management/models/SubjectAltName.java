// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.databind.annotation.JsonDeserialize;
import com.fasterxml.jackson.databind.annotation.JsonSerialize;

/**
 * A name to put in a {@code Server} certificate's {@code subjectAltName}.
 *
 * <p>Stated explicitly in the request, never read from a CSR: a CSR asking for a {@code
 * subjectAltName} extension is still refused. URI and e-mail names are not offered — nothing in
 * AXIAM consumes them yet.
 *
 * <p>Externally tagged: the wire shape names exactly one of the permitted keys below, with that
 * key's value directly beneath it -- never an empty object, and never a wrapper field naming which
 * arm this is. A closed set of shapes, sealed so a switch over the permitted records is exhaustive
 * and the compiler says so.
 */
@JsonSerialize(using = SubjectAltName.Serializer.class)
@JsonDeserialize(using = SubjectAltName.Deserializer.class)
public sealed interface SubjectAltName permits SubjectAltNameDns, SubjectAltNameIp {

    /** Writes the one field the matched arm names -- never {@code {}}. */
    final class Serializer extends com.fasterxml.jackson.databind.JsonSerializer<SubjectAltName> {
        /** Constructs the serializer Jackson instantiates from the class-level annotation. */
        public Serializer() {
        }

        @Override
        public void serialize(SubjectAltName value, com.fasterxml.jackson.core.JsonGenerator gen, com.fasterxml.jackson.databind.SerializerProvider serializers)
                throws java.io.IOException {
            gen.writeStartObject();
            if (value instanceof SubjectAltNameDns v) {
                gen.writeObjectField("dns", v.dns());
            }
            if (value instanceof SubjectAltNameIp v) {
                gen.writeObjectField("ip", v.ip());
            }
            gen.writeEndObject();
        }
    }

    /** Reads whichever one of the permitted keys is present; refuses neither/none. */
    final class Deserializer extends com.fasterxml.jackson.databind.JsonDeserializer<SubjectAltName> {
        /** Constructs the deserializer Jackson instantiates from the class-level annotation. */
        public Deserializer() {
        }

        @Override
        public SubjectAltName deserialize(com.fasterxml.jackson.core.JsonParser p, com.fasterxml.jackson.databind.DeserializationContext ctxt)
                throws java.io.IOException {
            com.fasterxml.jackson.databind.JsonNode node = p.getCodec().readTree(p);
            if (node.has("dns")) {
                return new SubjectAltNameDns(node.get("dns").asText());
            }
            if (node.has("ip")) {
                return new SubjectAltNameIp(node.get("ip").asText());
            }
            throw new java.io.IOException(
                    "SubjectAltName names none of the permitted keys: dns, ip");
        }
    }
}
