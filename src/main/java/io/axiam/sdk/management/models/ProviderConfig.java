// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * Provider-specific connection details.
 *
 * <p>A closed set of shapes discriminated by the kind field. Sealed, so a switch over the
 * permitted records is exhaustive and the compiler says so — which is what makes adding an arm a
 * compile error at every call site rather than a silent fall-through.
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.EXISTING_PROPERTY,
        property = "kind", visible = true)
@JsonSubTypes({
        @JsonSubTypes.Type(value = ProviderConfigSmtp.class, name = "smtp"),
        @JsonSubTypes.Type(value = ProviderConfigSendGrid.class, name = "send_grid"),
        @JsonSubTypes.Type(value = ProviderConfigPostmark.class, name = "postmark"),
        @JsonSubTypes.Type(value = ProviderConfigResend.class, name = "resend"),
        @JsonSubTypes.Type(value = ProviderConfigBrevo.class, name = "brevo"),
})
public sealed interface ProviderConfig permits ProviderConfigSmtp, ProviderConfigSendGrid, ProviderConfigPostmark, ProviderConfigResend, ProviderConfigBrevo {

    /**
     * Returns the discriminator this arm carries.
     *
     * @return the server's kind value for this arm
     */
    String kind();
}
