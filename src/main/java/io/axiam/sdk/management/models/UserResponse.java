// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

import com.fasterxml.jackson.databind.JsonNode;
import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * Public-safe user representation (no password_hash, no mfa_secret).
 *
 * @param createdAt the server's created_at field
 * @param email the server's email field
 * @param emailVerified True when the user's email address has been verified ({@code
 *     email_verified_at} is set). Exposed for the admin UI / profile.
 * @param failedLoginAttempts Number of consecutive failed login attempts.
 * @param id the server's id field
 * @param isLocked Whether the account is currently locked (locked_until is in the future).
 * @param lockedUntil Timestamp until which the account is locked, if any.
 * @param metadata the server's metadata field
 * @param mfaEnabled the server's mfa_enabled field
 * @param status the server's status field
 * @param tenantId the server's tenant_id field
 * @param updatedAt the server's updated_at field
 * @param username the server's username field
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record UserResponse(
        @JsonProperty("created_at") OffsetDateTime createdAt,
        @JsonProperty("email") String email,
        @JsonProperty("email_verified") Boolean emailVerified,
        @JsonProperty("failed_login_attempts") Integer failedLoginAttempts,
        @JsonProperty("id") UUID id,
        @JsonProperty("is_locked") Boolean isLocked,
        @JsonProperty("locked_until") @Nullable OffsetDateTime lockedUntil,
        @JsonProperty("metadata") JsonNode metadata,
        @JsonProperty("mfa_enabled") Boolean mfaEnabled,
        @JsonProperty("status") UserStatus status,
        @JsonProperty("tenant_id") UUID tenantId,
        @JsonProperty("updated_at") OffsetDateTime updatedAt,
        @JsonProperty("username") String username
) {
}
