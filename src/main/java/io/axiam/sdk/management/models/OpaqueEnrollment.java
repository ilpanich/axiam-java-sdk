// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;

/**
 * The client-supplied half of an OPAQUE enrolment, as it appears inside registration /
 * change-password / reset-completion / bootstrap request bodies.
 *
 * <p>There is no standalone {@code register/finish} endpoint, deliberately. A record can only be
 * created at a moment when the plaintext password legitimately exists on the client, and every one
 * of those moments is already an endpoint that takes a password. A free-standing finish would be
 * an endpoint whose only job is to attach a credential to an account, which is a thing worth not
 * having.
 *
 * <p>Kept separate from [{@code CreateOpaqueCredential}] because the tenant, the user and the
 * credential identifier are all decided by the server — a client that could name them could enrol
 * a record against somebody else's account.
 *
 * @param opaqueSession The {@code opaque_session} from the register/start response, echoed
 *     verbatim.
 * @param registrationRecord Lowercase-hex serialized RFC 9807 {@code RegistrationRecord}.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record OpaqueEnrollment(
        @JsonProperty("opaque_session") String opaqueSession,
        @JsonProperty("registration_record") String registrationRecord
) {
}
