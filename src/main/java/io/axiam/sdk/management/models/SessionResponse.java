// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

import java.util.List;
import java.util.UUID;

/**
 * One of a user's sessions, as an administrator sees it.
 *
 * @param amr RFC 8176 method references for that authentication.
 * @param authenticatedAt X7.2 — when the end user actually authenticated, which is not {@code
 *     created_at} on a session produced by refresh rotation.
 * @param createdAt the server's created_at field
 * @param expiresAt the server's expires_at field
 * @param id the server's id field
 * @param ipAddress the server's ip_address field
 * @param refreshReplayAt T-254 — when a refresh token of this session was last presented after it
 *     had already been rotated. {@code None} if that has never happened.
 * @param refreshReplayGraceAccepted T-254 — replays accepted under the FAPI 2.0 §5.3.2.1-9 grace
 *     window. Only ever non-zero for a client registered {@code profile: fapi2}.
 * @param refreshReplayRefused T-254 — replays refused because there was no window to accept them
 *     in. Nothing a conformant client does.
 * @param refreshReplayVerdict T-254 — the badge: {@code none}, {@code fapi_grace_retry} or {@code
 *     refused}. Derived from the two counters below rather than stored, so it cannot disagree with
 *     them. A refusal outranks an accepted grace retry however the counts compare.
 * @param userAgent the server's user_agent field
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record SessionResponse(
        @JsonProperty("amr") List<String> amr,
        @JsonProperty("authenticated_at") String authenticatedAt,
        @JsonProperty("created_at") String createdAt,
        @JsonProperty("expires_at") String expiresAt,
        @JsonProperty("id") UUID id,
        @JsonProperty("ip_address") @Nullable String ipAddress,
        @JsonProperty("refresh_replay_at") @Nullable String refreshReplayAt,
        @JsonProperty("refresh_replay_grace_accepted") Integer refreshReplayGraceAccepted,
        @JsonProperty("refresh_replay_refused") Integer refreshReplayRefused,
        @JsonProperty("refresh_replay_verdict") String refreshReplayVerdict,
        @JsonProperty("user_agent") @Nullable String userAgent
) {
}
