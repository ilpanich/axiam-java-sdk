// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

import java.util.UUID;

/**
 * One credential's compliance outcome (D9).
 *
 * @param aaguid the server's aaguid field
 * @param authenticatorName the server's authenticator_name field
 * @param compliant {@code false} only for a genuine policy violation ([{@code
 *     ComplianceStatus::NonCompliant}]) — a credential with no recorded AAGUID ([{@code
 *     ComplianceStatus::Unknown}], D9's pre-X3 case) is always {@code true} here, never reported as a
 *     violation.
 * @param credentialId the server's credential_id field
 * @param name The credential's user-assigned name ({@code WebauthnCredential::name}), not the
 *     owning user's account name.
 * @param reason {@code None} only when {@code compliant} and the credential has a recorded AAGUID;
 *     set for every non-compliant *and* every "unknown" (pre-X3) credential.
 * @param userId the server's user_id field
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record ComplianceReportEntry(
        @JsonProperty("aaguid") @Nullable UUID aaguid,
        @JsonProperty("authenticator_name") @Nullable String authenticatorName,
        @JsonProperty("compliant") Boolean compliant,
        @JsonProperty("credential_id") UUID credentialId,
        @JsonProperty("name") String name,
        @JsonProperty("reason") @Nullable String reason,
        @JsonProperty("user_id") UUID userId
) {
}
