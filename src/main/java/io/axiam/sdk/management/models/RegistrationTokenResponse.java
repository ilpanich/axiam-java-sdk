// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * Metadata only. The handle exists in plaintext exactly once, in [{@code
 * CreateRegistrationTokenResponse}].
 *
 * @param createdAt Row creation time.
 * @param createdBy The administrator who minted it.
 * @param expiresAt When it stops being usable.
 * @param id Row identity.
 * @param name The operator-facing label.
 * @param tenantId The tenant a registration on this token lands in.
 * @param usedAt When it was spent, if it was.
 * @param usedByClientId Reserved; always absent in this build. See {@code
 *     axiam_core::models::oauth2_registration_token::OAuth2RegistrationToken::used_by_client_id} — the
 *     registration a token produced is recorded in the audit log, not here.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record RegistrationTokenResponse(
        @JsonProperty("created_at") OffsetDateTime createdAt,
        @JsonProperty("created_by") UUID createdBy,
        @JsonProperty("expires_at") OffsetDateTime expiresAt,
        @JsonProperty("id") UUID id,
        @JsonProperty("name") String name,
        @JsonProperty("tenant_id") UUID tenantId,
        @JsonProperty("used_at") @Nullable OffsetDateTime usedAt,
        @JsonProperty("used_by_client_id") @Nullable String usedByClientId
) {
}
