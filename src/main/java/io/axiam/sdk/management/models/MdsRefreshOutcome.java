// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonSubTypes;
import com.fasterxml.jackson.annotation.JsonTypeInfo;

/**
 * {@code POST /api/v1/mds/refresh} response — the outcome of one ingestion attempt (mirrors {@code
 * axiam_db::mds_ingest::MdsIngestOutcome}).
 *
 * <p>A closed set of shapes discriminated by the outcome field. Sealed, so a switch over the
 * permitted records is exhaustive and the compiler says so — which is what makes adding an arm a
 * compile error at every call site rather than a silent fall-through.
 */
@JsonTypeInfo(use = JsonTypeInfo.Id.NAME, include = JsonTypeInfo.As.EXISTING_PROPERTY,
        property = "outcome", visible = true)
@JsonSubTypes({
        @JsonSubTypes.Type(value = MdsRefreshOutcomeInitial.class, name = "initial"),
        @JsonSubTypes.Type(value = MdsRefreshOutcomeReplaced.class, name = "replaced"),
        @JsonSubTypes.Type(value = MdsRefreshOutcomeNoOpRefresh.class, name = "no_op_refresh"),
        @JsonSubTypes.Type(value = MdsRefreshOutcomeRollbackRejected.class, name = "rollback_rejected"),
})
public sealed interface MdsRefreshOutcome permits MdsRefreshOutcomeInitial, MdsRefreshOutcomeReplaced, MdsRefreshOutcomeNoOpRefresh, MdsRefreshOutcomeRollbackRejected {

    /**
     * Returns the discriminator this arm carries.
     *
     * @return the server's outcome value for this arm
     */
    String outcome();
}
