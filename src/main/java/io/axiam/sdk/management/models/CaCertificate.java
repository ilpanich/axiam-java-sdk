// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management.models;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import org.jspecify.annotations.Nullable;

import java.time.OffsetDateTime;
import java.util.UUID;

/**
 * A CA (Certificate Authority) certificate at the organization level.
 *
 * <p>CA certificates are the root of trust for all tenant certificates within the organization.
 * Private keys for signing CAs are encrypted with AES-256-GCM and stored separately; non-signing
 * CAs only store the public certificate.
 *
 * @param chainPem The issuers above [{@code Self::public_cert_pem}], concatenated PEM, nearest
 *     issuer first and the root last. {@code None} for a CA that is its own root, which is every CA
 *     AXIAM generated before Vault's PKI engine was an option. Present for a {@code vault_pki} CA,
 *     where it is the only copy of the root certificate anything outside Vault will ever see — a
 *     relying party cannot validate an AXIAM-issued leaf without it, and {@code
 *     root/generate/internal} returns it exactly once.
 * @param createdAt the server's created_at field
 * @param fingerprint SHA-256 fingerprint of the certificate.
 * @param id the server's id field
 * @param keyAlgorithm the server's key_algorithm field
 * @param keyCustody Which custodian holds this CA's signing key. Recorded per CA rather than read
 *     from configuration, so adopting a new custodian does not strand the CAs that already exist. Not
 *     secret — an operator needs to see it, and it discloses only where a key is kept.
 * @param keyLocator Where the custodian put the key. A Vault path under its mount; {@code None}
 *     for database custody, whose locator is the row itself.
 * @param mtlsTrustAnchor Whether this CA is offered as a trust anchor for mutual TLS. When set,
 *     the server exports this CA's **public** certificate to the bundle named by {@code
 *     AXIAM__SERVER__TLS__CLIENT_CA_BUNDLE_PATH} at startup and turns client-certificate
 *     authentication on ({@code optional}) if the operator has not configured it explicitly. A client
 *     presenting a certificate that chains to this CA is then verified by the TLS layer itself, which
 *     is what {@code axiam_pki::mtls} needs to authenticate an IoT device or a service account by
 *     certificate rather than by secret. # What is and is not copied Only {@code public_cert_pem} —
 *     the certificate. The **private key stays where its custodian put it** (Vault, or sealed into the
 *     row) and is never written to the server volume. A trust anchor is public by construction: it is
 *     what the server hands every client during the TLS handshake, and every device that has to
 *     validate the chain already holds a copy. # Why a restart rustls builds its {@code RootCertStore}
 *     once, when the listener is constructed, and actix-web binds that config for the process's life.
 *     Toggling this changes what the *next* boot trusts, and the API says so in its response rather
 *     than pretending the change took effect. Defaults to {@code false}, so a deployment that never
 *     touches this keeps exactly the TLS posture it has today.
 * @param notAfter Validity end.
 * @param notBefore Validity start.
 * @param organizationId The organization this CA belongs to.
 * @param parentCaId The CA in this organization that signed this one. {@code None} for an
 *     organization-level CA, which is either self-signed or imported and has no parent inside AXIAM.
 * @param publicCertPem PEM-encoded public certificate. The certificate that *signs*, which under
 *     {@code vault_pki} custody is the intermediate rather than the root beneath which it was created.
 * @param status the server's status field
 * @param subject The certificate subject (e.g., {@code CN=ACME Corp Root CA}).
 * @param tenantId The tenant this CA signs for, when it is a tenant signing CA. {@code None} for
 *     an organization-level CA — the trust anchor, and the only kind that existed before tenant
 *     signing CAs. {@code Some} for an intermediate created under one, which exists so a tenant's
 *     user, service and device certificates chain through a CA that can be revoked and replaced
 *     without touching the anchor the rest of the estate trusts.
 */
@JsonIgnoreProperties(ignoreUnknown = true)
@JsonInclude(JsonInclude.Include.NON_NULL)
public record CaCertificate(
        @JsonProperty("chain_pem") @Nullable String chainPem,
        @JsonProperty("created_at") OffsetDateTime createdAt,
        @JsonProperty("fingerprint") String fingerprint,
        @JsonProperty("id") UUID id,
        @JsonProperty("key_algorithm") KeyAlgorithm keyAlgorithm,
        @JsonProperty("key_custody") @Nullable String keyCustody,
        @JsonProperty("key_locator") @Nullable String keyLocator,
        @JsonProperty("mtls_trust_anchor") @Nullable Boolean mtlsTrustAnchor,
        @JsonProperty("not_after") OffsetDateTime notAfter,
        @JsonProperty("not_before") OffsetDateTime notBefore,
        @JsonProperty("organization_id") UUID organizationId,
        @JsonProperty("parent_ca_id") @Nullable UUID parentCaId,
        @JsonProperty("public_cert_pem") String publicCertPem,
        @JsonProperty("status") CertificateStatus status,
        @JsonProperty("subject") String subject,
        @JsonProperty("tenant_id") @Nullable UUID tenantId
) {
}
