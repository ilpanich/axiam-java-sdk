// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management;

import com.fasterxml.jackson.databind.JsonNode;
import io.axiam.sdk.internal.ManagementTransport;
import org.jspecify.annotations.Nullable;

import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;

/**
 * The email_config management namespace (CONTRACT §27.1).
 *
 * <p>Transactional-mail transport, configurable at organization level and overridable per tenant.
 *
 * <p>Acquiring a handle performs no I/O and allocates nothing meaningful (§27.2 rule 1); it holds
 * the transport and cannot be constructed without one.
 *
 * <p>Every operation here is synchronous. The SDK's CompletableFuture twins exist only on the §1
 * core operations, so the async form of a management call is CompletableFuture.supplyAsync(()
 * -&gt; client.management().emailConfig()...) — one line, rather than 146 more methods to keep in
 * step.
 */
public final class EmailConfigApi {

    private final ManagementTransport transport;
    private final NamespaceScope scope;

    EmailConfigApi(ManagementTransport transport, NamespaceScope scope) {
        this.transport = transport;
        this.scope = scope;
    }

    /**
     * Addresses a different organization than the client's own.
     *
     * <p>§27.4 rule 3: the client's organization is the default, and a platform-admin token
     * legitimately overrides it.
     *
     * @param orgId the organization to address
     * @return a new handle; this one is unchanged
     */
    public EmailConfigApi inOrg(UUID orgId) {
        return new EmailConfigApi(transport, scope.withOrg(orgId));
    }

    /**
     * Addresses a different tenant than the client's own (§27.4 rule 3).
     *
     * @param tenantId the tenant to address
     * @return a new handle; this one is unchanged
     */
    public EmailConfigApi forTenant(UUID tenantId) {
        return new EmailConfigApi(transport, scope.withTenant(tenantId));
    }

    /**
     * Issues GET /api/v1/organizations/{org_id}/email-config.
     *
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.EmailConfig getOrg() {
        final String operation = "email_config.get_org";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/organizations/{org_id}/email-config", "/api/v1/organizations/" + orgId + "/email-config", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.EmailConfig.class, operation);
    }

    /**
     * Issues PUT /api/v1/organizations/{org_id}/email-config.
     *
     * <p>This is a REPLACEMENT, not a patch (§27.4 rule 5). Every field of the body is required,
     * and what you do not carry over from a prior read is not preserved -- it is overwritten. Read
     * first, change the field you mean, send the whole thing back.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.EmailConfig setOrg(io.axiam.sdk.management.models.SetOrgEmailConfig body) {
        final String operation = "email_config.set_org";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "PUT",
                "/api/v1/organizations/{org_id}/email-config", "/api/v1/organizations/" + orgId + "/email-config", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.EmailConfig.class, operation);
    }

    /**
     * Issues DELETE /api/v1/organizations/{org_id}/email-config.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void deleteOrg() {
        final String operation = "email_config.delete_org";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        transport.send(operation, "DELETE",
                "/api/v1/organizations/{org_id}/email-config", "/api/v1/organizations/" + orgId + "/email-config", query, null);
    }

    /**
     * Issues POST /api/v1/organizations/{org_id}/email-config/test.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.EmailTestResult testOrg() {
        final String operation = "email_config.test_org";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "POST",
                "/api/v1/organizations/{org_id}/email-config/test", "/api/v1/organizations/" + orgId + "/email-config/test", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.EmailTestResult.class, operation);
    }

    /**
     * Issues GET /api/v1/tenants/{tenant_id}/email-config.
     *
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.EmailConfigOverride getTenant() {
        final String operation = "email_config.get_tenant";
        UUID tenantId = ManagementSupport.resolveTenant(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/tenants/{tenant_id}/email-config", "/api/v1/tenants/" + tenantId + "/email-config", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.EmailConfigOverride.class, operation);
    }

    /**
     * Issues PUT /api/v1/tenants/{tenant_id}/email-config.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.EmailConfigOverride setTenant(io.axiam.sdk.management.models.EmailConfigOverride body) {
        final String operation = "email_config.set_tenant";
        UUID tenantId = ManagementSupport.resolveTenant(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "PUT",
                "/api/v1/tenants/{tenant_id}/email-config", "/api/v1/tenants/" + tenantId + "/email-config", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.EmailConfigOverride.class, operation);
    }

    /**
     * Issues DELETE /api/v1/tenants/{tenant_id}/email-config.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void deleteTenant() {
        final String operation = "email_config.delete_tenant";
        UUID tenantId = ManagementSupport.resolveTenant(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        transport.send(operation, "DELETE",
                "/api/v1/tenants/{tenant_id}/email-config", "/api/v1/tenants/" + tenantId + "/email-config", query, null);
    }

    /**
     * Issues POST /api/v1/tenants/{tenant_id}/email-config/test.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.EmailTestResult testTenant() {
        final String operation = "email_config.test_tenant";
        UUID tenantId = ManagementSupport.resolveTenant(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "POST",
                "/api/v1/tenants/{tenant_id}/email-config/test", "/api/v1/tenants/" + tenantId + "/email-config/test", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.EmailTestResult.class, operation);
    }

}
