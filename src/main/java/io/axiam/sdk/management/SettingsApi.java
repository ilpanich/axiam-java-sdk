// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management;

import com.fasterxml.jackson.databind.JsonNode;
import io.axiam.sdk.internal.ManagementTransport;
import org.jspecify.annotations.Nullable;

import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;

/**
 * The settings management namespace (CONTRACT §27.1).
 *
 * <p>Effective settings, and the organization/tenant layers they resolve from.
 *
 * <p>Acquiring a handle performs no I/O and allocates nothing meaningful (§27.2 rule 1); it holds
 * the transport and cannot be constructed without one.
 *
 * <p>Every operation here is synchronous. The SDK's CompletableFuture twins exist only on the §1
 * core operations, so the async form of a management call is CompletableFuture.supplyAsync(()
 * -&gt; client.management().settings()...) — one line, rather than 146 more methods to keep in
 * step.
 */
public final class SettingsApi {

    private final ManagementTransport transport;
    private final NamespaceScope scope;

    SettingsApi(ManagementTransport transport, NamespaceScope scope) {
        this.transport = transport;
        this.scope = scope;
    }

    /**
     * Addresses a different organization than the client's own.
     *
     * <p>§27.4 rule 3: the client's organization is the default, and a platform-admin token
     * legitimately overrides it.
     *
     * @param orgId the organization to address
     * @return a new handle; this one is unchanged
     */
    public SettingsApi inOrg(UUID orgId) {
        return new SettingsApi(transport, scope.withOrg(orgId));
    }

    /**
     * Addresses a different tenant than the client's own (§27.4 rule 3).
     *
     * @param tenantId the tenant to address
     * @return a new handle; this one is unchanged
     */
    public SettingsApi forTenant(UUID tenantId) {
        return new SettingsApi(transport, scope.withTenant(tenantId));
    }

    /**
     * Issues GET /api/v1/organizations/{org_id}/settings.
     *
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.SecuritySettings getOrg() {
        final String operation = "settings.get_org";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/organizations/{org_id}/settings", "/api/v1/organizations/" + orgId + "/settings", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.SecuritySettings.class, operation);
    }

    /**
     * Issues PUT /api/v1/organizations/{org_id}/settings.
     *
     * <p>This is a REPLACEMENT, not a patch (§27.4 rule 5). Every field of the body is required,
     * and what you do not carry over from a prior read is not preserved -- it is overwritten. Read
     * first, change the field you mean, send the whole thing back.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.SecuritySettings setOrg(io.axiam.sdk.management.models.SetOrgSettings body) {
        final String operation = "settings.set_org";
        UUID orgId = ManagementSupport.resolveOrg(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "PUT",
                "/api/v1/organizations/{org_id}/settings", "/api/v1/organizations/" + orgId + "/settings", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.SecuritySettings.class, operation);
    }

    /**
     * Issues GET /api/v1/settings.
     *
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.SecuritySettings getEffective() {
        final String operation = "settings.get_effective";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/settings", "/api/v1/settings", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.SecuritySettings.class, operation);
    }

    /**
     * Issues PUT /api/v1/settings.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.SecuritySettings setEffective(io.axiam.sdk.management.models.TenantSettingsOverride body) {
        final String operation = "settings.set_effective";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "PUT",
                "/api/v1/settings", "/api/v1/settings", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.SecuritySettings.class, operation);
    }

    /**
     * Issues GET /api/v1/tenants/{tenant_id}/settings.
     *
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.TenantSettingsOverride getTenantOverride() {
        final String operation = "settings.get_tenant_override";
        UUID tenantId = ManagementSupport.resolveTenant(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/tenants/{tenant_id}/settings", "/api/v1/tenants/" + tenantId + "/settings", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.TenantSettingsOverride.class, operation);
    }

    /**
     * Issues PUT /api/v1/tenants/{tenant_id}/settings.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.TenantSettingsOverride setTenantOverride(io.axiam.sdk.management.models.TenantSettingsOverride body) {
        final String operation = "settings.set_tenant_override";
        UUID tenantId = ManagementSupport.resolveTenant(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "PUT",
                "/api/v1/tenants/{tenant_id}/settings", "/api/v1/tenants/" + tenantId + "/settings", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.TenantSettingsOverride.class, operation);
    }

    /**
     * Issues DELETE /api/v1/tenants/{tenant_id}/settings.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void deleteTenantOverride() {
        final String operation = "settings.delete_tenant_override";
        UUID tenantId = ManagementSupport.resolveTenant(transport, scope, operation);
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        transport.send(operation, "DELETE",
                "/api/v1/tenants/{tenant_id}/settings", "/api/v1/tenants/" + tenantId + "/settings", query, null);
    }

}
