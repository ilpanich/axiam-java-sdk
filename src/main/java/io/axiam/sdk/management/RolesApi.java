// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management;

import com.fasterxml.jackson.databind.JsonNode;
import io.axiam.sdk.internal.ManagementTransport;
import org.jspecify.annotations.Nullable;

import java.util.LinkedHashMap;
import java.util.Map;
import java.util.UUID;

/**
 * The roles management namespace (CONTRACT §27.1).
 *
 * <p>Roles, their permission sets, and their assignment to users and groups.
 *
 * <p>Acquiring a handle performs no I/O and allocates nothing meaningful (§27.2 rule 1); it holds
 * the transport and cannot be constructed without one.
 *
 * <p>Every operation here is synchronous. The SDK's CompletableFuture twins exist only on the §1
 * core operations, so the async form of a management call is CompletableFuture.supplyAsync(()
 * -&gt; client.management().roles()...) — one line, rather than 147 more methods to keep in step.
 */
public final class RolesApi {

    private final ManagementTransport transport;
    private final NamespaceScope scope;

    RolesApi(ManagementTransport transport, NamespaceScope scope) {
        this.transport = transport;
        this.scope = scope;
    }

    /**
     * Issues GET /api/v1/roles.
     *
     * @param page where the page starts and how big it is, or null for the server's default
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public Page<io.axiam.sdk.management.models.Role> list(@Nullable PageRequest page) {
        final String operation = "roles.list";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        ManagementSupport.pageQuery(query, page);
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/roles", "/api/v1/roles", query, null);
        return ManagementSupport.convertPage(node, io.axiam.sdk.management.models.Role.class, operation);
    }

    /**
     * Walks roles.list to exhaustion, concatenating every page.
     *
     * <p>The auto-paging form §27.4 rule 4 requires. It stops on an empty page even if the total
     * disagrees, so a misreporting server costs one wasted request rather than an unbounded loop.
     *
     * @param start where the walk begins, or null to start at the top
     * @return every item in the set, across as many requests as it takes
     */
    public java.util.List<io.axiam.sdk.management.models.Role> listAll(@Nullable PageRequest start) {
        return ManagementSupport.collectPages(start, p -> list(p));
    }

    /**
     * Issues POST /api/v1/roles.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.Role create(io.axiam.sdk.management.models.CreateRoleRequest body) {
        final String operation = "roles.create";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "POST",
                "/api/v1/roles", "/api/v1/roles", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.Role.class, operation);
    }

    /**
     * Issues GET /api/v1/roles/{role_id}.
     *
     * @param roleId the role_id path parameter
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.Role get(java.util.UUID roleId) {
        final String operation = "roles.get";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/roles/{role_id}", "/api/v1/roles/" + roleId + "", query, null);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.Role.class, operation);
    }

    /**
     * Issues PUT /api/v1/roles/{role_id}.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param roleId the role_id path parameter
     * @param body the request body
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public io.axiam.sdk.management.models.Role update(java.util.UUID roleId, io.axiam.sdk.management.models.UpdateRole body) {
        final String operation = "roles.update";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "PUT",
                "/api/v1/roles/{role_id}", "/api/v1/roles/" + roleId + "", query, body);
        return ManagementSupport.convert(node, io.axiam.sdk.management.models.Role.class, operation);
    }

    /**
     * Issues DELETE /api/v1/roles/{role_id}.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param roleId the role_id path parameter
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void delete(java.util.UUID roleId) {
        final String operation = "roles.delete";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        transport.send(operation, "DELETE",
                "/api/v1/roles/{role_id}", "/api/v1/roles/" + roleId + "", query, null);
    }

    /**
     * Issues GET /api/v1/roles/{role_id}/users.
     *
     * @param roleId the role_id path parameter
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public java.util.List<io.axiam.sdk.management.models.RoleUserAssignment> listUsers(java.util.UUID roleId) {
        final String operation = "roles.list_users";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/roles/{role_id}/users", "/api/v1/roles/" + roleId + "/users", query, null);
        return ManagementSupport.convertList(node, io.axiam.sdk.management.models.RoleUserAssignment.class, operation);
    }

    /**
     * Issues POST /api/v1/roles/{role_id}/users.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param roleId the role_id path parameter
     * @param body the request body
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void assignToUser(java.util.UUID roleId, io.axiam.sdk.management.models.AssignRoleToUserRequest body) {
        final String operation = "roles.assign_to_user";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        transport.send(operation, "POST",
                "/api/v1/roles/{role_id}/users", "/api/v1/roles/" + roleId + "/users", query, body);
    }

    /**
     * Issues DELETE /api/v1/roles/{role_id}/users/{user_id}.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param roleId the role_id path parameter
     * @param userId the user_id path parameter
     * @param resourceId the optional resource_id query filter, or null to omit it
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void unassignFromUser(java.util.UUID roleId, java.util.UUID userId, @Nullable String resourceId) {
        final String operation = "roles.unassign_from_user";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        query.put("resource_id", resourceId);
        transport.send(operation, "DELETE",
                "/api/v1/roles/{role_id}/users/{user_id}", "/api/v1/roles/" + roleId + "/users/" + userId + "", query, null);
    }

    /**
     * Issues GET /api/v1/roles/{role_id}/groups.
     *
     * @param roleId the role_id path parameter
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public java.util.List<io.axiam.sdk.management.models.RoleGroupAssignment> listGroups(java.util.UUID roleId) {
        final String operation = "roles.list_groups";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/roles/{role_id}/groups", "/api/v1/roles/" + roleId + "/groups", query, null);
        return ManagementSupport.convertList(node, io.axiam.sdk.management.models.RoleGroupAssignment.class, operation);
    }

    /**
     * Issues POST /api/v1/roles/{role_id}/groups.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param roleId the role_id path parameter
     * @param body the request body
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void assignToGroup(java.util.UUID roleId, io.axiam.sdk.management.models.AssignRoleToGroupRequest body) {
        final String operation = "roles.assign_to_group";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        transport.send(operation, "POST",
                "/api/v1/roles/{role_id}/groups", "/api/v1/roles/" + roleId + "/groups", query, body);
    }

    /**
     * Issues DELETE /api/v1/roles/{role_id}/groups/{group_id}.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param roleId the role_id path parameter
     * @param groupId the group_id path parameter
     * @param resourceId the optional resource_id query filter, or null to omit it
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void unassignFromGroup(java.util.UUID roleId, java.util.UUID groupId, @Nullable String resourceId) {
        final String operation = "roles.unassign_from_group";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        query.put("resource_id", resourceId);
        transport.send(operation, "DELETE",
                "/api/v1/roles/{role_id}/groups/{group_id}", "/api/v1/roles/" + roleId + "/groups/" + groupId + "", query, null);
    }

    /**
     * Issues GET /api/v1/roles/{role_id}/permissions.
     *
     * @param roleId the role_id path parameter
     * @return the server's response
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public java.util.List<io.axiam.sdk.management.models.ResolvedPermissionGrant> listPermissions(java.util.UUID roleId) {
        final String operation = "roles.list_permissions";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        JsonNode node = transport.send(operation, "GET",
                "/api/v1/roles/{role_id}/permissions", "/api/v1/roles/" + roleId + "/permissions", query, null);
        return ManagementSupport.convertList(node, io.axiam.sdk.management.models.ResolvedPermissionGrant.class, operation);
    }

    /**
     * Issues POST /api/v1/roles/{role_id}/permissions.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param roleId the role_id path parameter
     * @param body the request body
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void grantPermission(java.util.UUID roleId, io.axiam.sdk.management.models.GrantPermissionRequest body) {
        final String operation = "roles.grant_permission";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        transport.send(operation, "POST",
                "/api/v1/roles/{role_id}/permissions", "/api/v1/roles/" + roleId + "/permissions", query, body);
    }

    /**
     * Issues DELETE /api/v1/roles/{role_id}/permissions/{permission_id}.
     *
     * <p>Not retried on failure (§27.4 rule 8): every write on this surface is issued exactly
     * once, including the ones that look idempotent.
     *
     * @param roleId the role_id path parameter
     * @param permissionId the permission_id path parameter
     * @throws io.axiam.sdk.errors.AuthError if there is no active session (§27.4 rule 1)
     * @throws io.axiam.sdk.errors.NotFoundError if the server answers 404
     * @throws io.axiam.sdk.errors.ConflictError if the server answers 409
     * @throws io.axiam.sdk.errors.ValidationError if the server answers 400 or 422
     * @throws io.axiam.sdk.errors.NetworkError on a transport failure or any other unsuccessful
     *     status
     */
    public void revokePermission(java.util.UUID roleId, java.util.UUID permissionId) {
        final String operation = "roles.revoke_permission";
        Map<String, @Nullable String> query = new LinkedHashMap<>();
        transport.send(operation, "DELETE",
                "/api/v1/roles/{role_id}/permissions/{permission_id}", "/api/v1/roles/" + roleId + "/permissions/" + permissionId + "", query, null);
    }

}
