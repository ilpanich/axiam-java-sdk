// @generated by scripts/gen_management.py from management-registry.json and
// openapi.json. DO NOT EDIT -- your changes will be overwritten. Regenerate with
// `python3 scripts/gen_management.py`; CI verifies the committed output is current.
package io.axiam.sdk.management;

import com.fasterxml.jackson.databind.JsonNode;
import com.fasterxml.jackson.databind.ObjectMapper;
import com.fasterxml.jackson.datatype.jsr310.JavaTimeModule;
import io.axiam.sdk.Sensitive;
import io.axiam.sdk.internal.ManagementTransport;
import io.axiam.sdk.management.models.ApiProviderConfig;
import io.axiam.sdk.management.models.ClientAuthMethod;
import io.axiam.sdk.management.models.ClientProfile;
import io.axiam.sdk.management.models.EmailConfigOverride;
import io.axiam.sdk.management.models.FailurePolicy;
import io.axiam.sdk.management.models.ProviderConfigSmtp;
import io.axiam.sdk.management.models.ReactorMode;
import io.axiam.sdk.management.models.RetryPolicy;
import io.axiam.sdk.management.models.TenantSettingsOverride;
import io.axiam.sdk.management.models.TenantStatus;
import io.axiam.sdk.management.models.TokenExchangeTrustRequest;
import io.axiam.sdk.management.models.UpdateFederationConfigRequest;
import io.axiam.sdk.management.models.UpdateGroup;
import io.axiam.sdk.management.models.UpdateNotificationRuleRequest;
import io.axiam.sdk.management.models.UpdateOAuth2ClientRequest;
import io.axiam.sdk.management.models.UpdateOrganizationRequest;
import io.axiam.sdk.management.models.UpdatePermissionRequest;
import io.axiam.sdk.management.models.UpdateReactorRequest;
import io.axiam.sdk.management.models.UpdateResourceRequest;
import io.axiam.sdk.management.models.UpdateRole;
import io.axiam.sdk.management.models.UpdateScopeRequest;
import io.axiam.sdk.management.models.UpdateServiceAccount;
import io.axiam.sdk.management.models.UpdateTenant;
import io.axiam.sdk.management.models.UpdateUserRequest;
import io.axiam.sdk.management.models.UpdateWebhookRequest;
import io.axiam.sdk.management.models.UserStatus;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;
import java.util.List;
import java.util.UUID;

import static org.junit.jupiter.api.Assertions.assertEquals;

/**
 * Generated §27.4 rule 5 conformance: a sparse body sends what was set, and nothing else.
 *
 * <p>Rule 5 draws the line between a sparse update and a replacement at ABSENCE, not at null: a
 * field the caller did not set must not appear in the body at all, because a null on the wire is a
 * request to CLEAR the field. One hand-written example proves that for one model; the failure this
 * guards against is a generator that gets it right for the model somebody wrote a test for and
 * wrong for the other seventeen.
 *
 * <p>Each case sets one field at a time and asserts the body carries exactly that one wire key,
 * then sets every field and asserts the body carries exactly all of them. Bodies are rendered
 * through {&#64;link ManagementTransport#encodeBody}, the writer the SDK actually sends with.
 */
class ManagementSparseBodiesGeneratedTest {

    /** Reads back what the wire writer produced. */
    private static final ObjectMapper JSON = new ObjectMapper().registerModule(new JavaTimeModule());

    /** Stands in for any {@code uuid}-formatted component. */
    private static final UUID EXAMPLE_ID = UUID.fromString("11111111-1111-4111-8111-111111111111");

    /** §27.4 rule 5 for ApiProviderConfig: each setter sets exactly its own key. */
    @Test
    void apiProviderConfigSendsOnlyWhatWasSet() throws Exception {
        assertKeys(ApiProviderConfig.builder()
                .apiUrl("example").build(), "api_url");
        assertKeys(ApiProviderConfig.builder()
                .apiUrl("example")
                .build(),
                "api_url");
        assertKeys(ApiProviderConfig.builder().build());
    }

    /** §27.4 rule 5 for EmailConfigOverride: each setter sets exactly its own key. */
    @Test
    void emailConfigOverrideSendsOnlyWhatWasSet() throws Exception {
        assertKeys(EmailConfigOverride.builder()
                .enabled(true).build(), "enabled");
        assertKeys(EmailConfigOverride.builder()
                .fromEmail("example").build(), "from_email");
        assertKeys(EmailConfigOverride.builder()
                .fromName("example").build(), "from_name");
        assertKeys(EmailConfigOverride.builder()
                .provider(new ProviderConfigSmtp("smtp", "example", 1, true, "example")).build(), "provider");
        assertKeys(EmailConfigOverride.builder()
                .replyTo("example").build(), "reply_to");
        assertKeys(EmailConfigOverride.builder()
                .enabled(true)
                .fromEmail("example")
                .fromName("example")
                .provider(new ProviderConfigSmtp("smtp", "example", 1, true, "example"))
                .replyTo("example")
                .build(),
                "enabled", "from_email", "from_name", "provider", "reply_to");
        assertKeys(EmailConfigOverride.builder().build());
    }

    /** §27.4 rule 5 for TenantSettingsOverride: each setter sets exactly its own key. */
    @Test
    void tenantSettingsOverrideSendsOnlyWhatWasSet() throws Exception {
        assertKeys(TenantSettingsOverride.builder()
                .accessTokenLifetimeSecs(1L).build(), "access_token_lifetime_secs");
        assertKeys(TenantSettingsOverride.builder()
                .adminNotificationsEnabled(true).build(), "admin_notifications_enabled");
        assertKeys(TenantSettingsOverride.builder()
                .defaultCertValidityDays(1).build(), "default_cert_validity_days");
        assertKeys(TenantSettingsOverride.builder()
                .deletionGracePeriodDays(1).build(), "deletion_grace_period_days");
        assertKeys(TenantSettingsOverride.builder()
                .emailVerificationGracePeriodHours(1).build(), "email_verification_grace_period_hours");
        assertKeys(TenantSettingsOverride.builder()
                .emailVerificationRequired(true).build(), "email_verification_required");
        assertKeys(TenantSettingsOverride.builder()
                .hibpCheckEnabled(true).build(), "hibp_check_enabled");
        assertKeys(TenantSettingsOverride.builder()
                .lockoutBackoffMultiplier(1.0).build(), "lockout_backoff_multiplier");
        assertKeys(TenantSettingsOverride.builder()
                .lockoutDurationSecs(1L).build(), "lockout_duration_secs");
        assertKeys(TenantSettingsOverride.builder()
                .maxCertValidityDays(1).build(), "max_cert_validity_days");
        assertKeys(TenantSettingsOverride.builder()
                .maxFailedLoginAttempts(1).build(), "max_failed_login_attempts");
        assertKeys(TenantSettingsOverride.builder()
                .maxLockoutDurationSecs(1L).build(), "max_lockout_duration_secs");
        assertKeys(TenantSettingsOverride.builder()
                .mfaChallengeLifetimeSecs(1L).build(), "mfa_challenge_lifetime_secs");
        assertKeys(TenantSettingsOverride.builder()
                .mfaEnforced(true).build(), "mfa_enforced");
        assertKeys(TenantSettingsOverride.builder()
                .minLength(1).build(), "min_length");
        assertKeys(TenantSettingsOverride.builder()
                .opaqueKsf("example").build(), "opaque_ksf");
        assertKeys(TenantSettingsOverride.builder()
                .opaqueMode("example").build(), "opaque_mode");
        assertKeys(TenantSettingsOverride.builder()
                .opaqueSuite("example").build(), "opaque_suite");
        assertKeys(TenantSettingsOverride.builder()
                .passwordHistoryCount(1).build(), "password_history_count");
        assertKeys(TenantSettingsOverride.builder()
                .refreshTokenLifetimeSecs(1L).build(), "refresh_token_lifetime_secs");
        assertKeys(TenantSettingsOverride.builder()
                .requireDigits(true).build(), "require_digits");
        assertKeys(TenantSettingsOverride.builder()
                .requireLowercase(true).build(), "require_lowercase");
        assertKeys(TenantSettingsOverride.builder()
                .requireSymbols(true).build(), "require_symbols");
        assertKeys(TenantSettingsOverride.builder()
                .requireUppercase(true).build(), "require_uppercase");
        assertKeys(TenantSettingsOverride.builder()
                .webauthnUserVerification("example").build(), "webauthn_user_verification");
        assertKeys(TenantSettingsOverride.builder()
                .accessTokenLifetimeSecs(1L)
                .adminNotificationsEnabled(true)
                .defaultCertValidityDays(1)
                .deletionGracePeriodDays(1)
                .emailVerificationGracePeriodHours(1)
                .emailVerificationRequired(true)
                .hibpCheckEnabled(true)
                .lockoutBackoffMultiplier(1.0)
                .lockoutDurationSecs(1L)
                .maxCertValidityDays(1)
                .maxFailedLoginAttempts(1)
                .maxLockoutDurationSecs(1L)
                .mfaChallengeLifetimeSecs(1L)
                .mfaEnforced(true)
                .minLength(1)
                .opaqueKsf("example")
                .opaqueMode("example")
                .opaqueSuite("example")
                .passwordHistoryCount(1)
                .refreshTokenLifetimeSecs(1L)
                .requireDigits(true)
                .requireLowercase(true)
                .requireSymbols(true)
                .requireUppercase(true)
                .webauthnUserVerification("example")
                .build(),
                "access_token_lifetime_secs", "admin_notifications_enabled", "default_cert_validity_days",
                "deletion_grace_period_days", "email_verification_grace_period_hours",
                "email_verification_required", "hibp_check_enabled", "lockout_backoff_multiplier",
                "lockout_duration_secs", "max_cert_validity_days", "max_failed_login_attempts",
                "max_lockout_duration_secs", "mfa_challenge_lifetime_secs", "mfa_enforced",
                "min_length", "opaque_ksf", "opaque_mode", "opaque_suite", "password_history_count",
                "refresh_token_lifetime_secs", "require_digits", "require_lowercase",
                "require_symbols", "require_uppercase", "webauthn_user_verification");
        assertKeys(TenantSettingsOverride.builder().build());
    }

    /** §27.4 rule 5 for TokenExchangeTrustRequest: each setter sets exactly its own key. */
    @Test
    void tokenExchangeTrustRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(TokenExchangeTrustRequest.builder()
                .acceptedAudiences(java.util.List.of()).build(), "accepted_audiences");
        assertKeys(TokenExchangeTrustRequest.builder()
                .enabled(true).build(), "enabled");
        assertKeys(TokenExchangeTrustRequest.builder()
                .maxLifetimeSecs(1L).build(), "max_lifetime_secs");
        assertKeys(TokenExchangeTrustRequest.builder()
                .maxTokenAgeSecs(1L).build(), "max_token_age_secs");
        assertKeys(TokenExchangeTrustRequest.builder()
                .scopeMap(JSON.createObjectNode()).build(), "scope_map");
        assertKeys(TokenExchangeTrustRequest.builder()
                .subjectMapping("example").build(), "subject_mapping");
        assertKeys(TokenExchangeTrustRequest.builder()
                .acceptedAudiences(java.util.List.of())
                .enabled(true)
                .maxLifetimeSecs(1L)
                .maxTokenAgeSecs(1L)
                .scopeMap(JSON.createObjectNode())
                .subjectMapping("example")
                .build(),
                "accepted_audiences", "enabled", "max_lifetime_secs", "max_token_age_secs",
                "scope_map", "subject_mapping");
        assertKeys(TokenExchangeTrustRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdateFederationConfigRequest: each setter sets exactly its own key. */
    @Test
    void updateFederationConfigRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateFederationConfigRequest.builder()
                .allowTenantInheritance(true).build(), "allow_tenant_inheritance");
        assertKeys(UpdateFederationConfigRequest.builder()
                .allowedAlgorithms(java.util.List.of()).build(), "allowed_algorithms");
        assertKeys(UpdateFederationConfigRequest.builder()
                .allowedIssuerTenants(java.util.List.of()).build(), "allowed_issuer_tenants");
        assertKeys(UpdateFederationConfigRequest.builder()
                .appleKeyId("example").build(), "apple_key_id");
        assertKeys(UpdateFederationConfigRequest.builder()
                .appleTeamId("example").build(), "apple_team_id");
        assertKeys(UpdateFederationConfigRequest.builder()
                .attributeMap(JSON.createObjectNode()).build(), "attribute_map");
        assertKeys(UpdateFederationConfigRequest.builder()
                .authorizationEndpoint("example").build(), "authorization_endpoint");
        assertKeys(UpdateFederationConfigRequest.builder()
                .buttonIcon("example").build(), "button_icon");
        assertKeys(UpdateFederationConfigRequest.builder()
                .clientId("example").build(), "client_id");
        assertKeys(UpdateFederationConfigRequest.builder()
                .clientSecret(Sensitive.of("example")).build(), "client_secret");
        assertKeys(UpdateFederationConfigRequest.builder()
                .enabled(true).build(), "enabled");
        assertKeys(UpdateFederationConfigRequest.builder()
                .idpSigningCertPem("example").build(), "idp_signing_cert_pem");
        assertKeys(UpdateFederationConfigRequest.builder()
                .metadataUrl("example").build(), "metadata_url");
        assertKeys(UpdateFederationConfigRequest.builder()
                .provider("example").build(), "provider");
        assertKeys(UpdateFederationConfigRequest.builder()
                .providerSlug("example").build(), "provider_slug");
        assertKeys(UpdateFederationConfigRequest.builder()
                .requirePkce(true).build(), "require_pkce");
        assertKeys(UpdateFederationConfigRequest.builder()
                .scopes(java.util.List.of()).build(), "scopes");
        assertKeys(UpdateFederationConfigRequest.builder()
                .tokenEndpoint("example").build(), "token_endpoint");
        assertKeys(UpdateFederationConfigRequest.builder()
                .tokenExchange(new TokenExchangeTrustRequest(null, null, null, null, null, null)).build(), "token_exchange");
        assertKeys(UpdateFederationConfigRequest.builder()
                .userinfoEndpoint("example").build(), "userinfo_endpoint");
        assertKeys(UpdateFederationConfigRequest.builder()
                .allowTenantInheritance(true)
                .allowedAlgorithms(java.util.List.of())
                .allowedIssuerTenants(java.util.List.of())
                .appleKeyId("example")
                .appleTeamId("example")
                .attributeMap(JSON.createObjectNode())
                .authorizationEndpoint("example")
                .buttonIcon("example")
                .clientId("example")
                .clientSecret(Sensitive.of("example"))
                .enabled(true)
                .idpSigningCertPem("example")
                .metadataUrl("example")
                .provider("example")
                .providerSlug("example")
                .requirePkce(true)
                .scopes(java.util.List.of())
                .tokenEndpoint("example")
                .tokenExchange(new TokenExchangeTrustRequest(null, null, null, null, null, null))
                .userinfoEndpoint("example")
                .build(),
                "allow_tenant_inheritance", "allowed_algorithms", "allowed_issuer_tenants",
                "apple_key_id", "apple_team_id", "attribute_map", "authorization_endpoint",
                "button_icon", "client_id", "client_secret", "enabled", "idp_signing_cert_pem",
                "metadata_url", "provider", "provider_slug", "require_pkce", "scopes",
                "token_endpoint", "token_exchange", "userinfo_endpoint");
        assertKeys(UpdateFederationConfigRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdateGroup: each setter sets exactly its own key. */
    @Test
    void updateGroupSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateGroup.builder()
                .description("example").build(), "description");
        assertKeys(UpdateGroup.builder()
                .metadata(JSON.createObjectNode()).build(), "metadata");
        assertKeys(UpdateGroup.builder()
                .name("example").build(), "name");
        assertKeys(UpdateGroup.builder()
                .description("example")
                .metadata(JSON.createObjectNode())
                .name("example")
                .build(),
                "description", "metadata", "name");
        assertKeys(UpdateGroup.builder().build());
    }

    /** §27.4 rule 5 for UpdateNotificationRuleRequest: each setter sets exactly its own key. */
    @Test
    void updateNotificationRuleRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateNotificationRuleRequest.builder()
                .description("example").build(), "description");
        assertKeys(UpdateNotificationRuleRequest.builder()
                .enabled(true).build(), "enabled");
        assertKeys(UpdateNotificationRuleRequest.builder()
                .events(java.util.List.of()).build(), "events");
        assertKeys(UpdateNotificationRuleRequest.builder()
                .name("example").build(), "name");
        assertKeys(UpdateNotificationRuleRequest.builder()
                .recipientEmails(java.util.List.of()).build(), "recipient_emails");
        assertKeys(UpdateNotificationRuleRequest.builder()
                .description("example")
                .enabled(true)
                .events(java.util.List.of())
                .name("example")
                .recipientEmails(java.util.List.of())
                .build(),
                "description", "enabled", "events", "name", "recipient_emails");
        assertKeys(UpdateNotificationRuleRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdateOAuth2ClientRequest: each setter sets exactly its own key. */
    @Test
    void updateOAuth2ClientRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .backchannelLogoutUri("example").build(), "backchannel_logout_uri");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .dpopBoundAccessTokens(true).build(), "dpop_bound_access_tokens");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .dpopRequireNonce(true).build(), "dpop_require_nonce");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .grantTypes(java.util.List.of()).build(), "grant_types");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .jwks("example").build(), "jwks");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .jwksUri("example").build(), "jwks_uri");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .name("example").build(), "name");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .postLogoutRedirectUris(java.util.List.of()).build(), "post_logout_redirect_uris");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .profile(ClientProfile.STANDARD).build(), "profile");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .redirectUris(java.util.List.of()).build(), "redirect_uris");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .requirePar(true).build(), "require_par");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .scopes(java.util.List.of()).build(), "scopes");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .selfSignedTlsClientAuthThumbprints(java.util.List.of()).build(), "self_signed_tls_client_auth_thumbprints");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .tlsClientAuthSanDns("example").build(), "tls_client_auth_san_dns");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .tlsClientAuthSanUri("example").build(), "tls_client_auth_san_uri");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .tlsClientAuthSubjectDn("example").build(), "tls_client_auth_subject_dn");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .tlsClientCertificateBoundAccessTokens(true).build(), "tls_client_certificate_bound_access_tokens");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .tokenEndpointAuthMethod(ClientAuthMethod.CLIENT_SECRET_POST).build(), "token_endpoint_auth_method");
        assertKeys(UpdateOAuth2ClientRequest.builder()
                .backchannelLogoutUri("example")
                .dpopBoundAccessTokens(true)
                .dpopRequireNonce(true)
                .grantTypes(java.util.List.of())
                .jwks("example")
                .jwksUri("example")
                .name("example")
                .postLogoutRedirectUris(java.util.List.of())
                .profile(ClientProfile.STANDARD)
                .redirectUris(java.util.List.of())
                .requirePar(true)
                .scopes(java.util.List.of())
                .selfSignedTlsClientAuthThumbprints(java.util.List.of())
                .tlsClientAuthSanDns("example")
                .tlsClientAuthSanUri("example")
                .tlsClientAuthSubjectDn("example")
                .tlsClientCertificateBoundAccessTokens(true)
                .tokenEndpointAuthMethod(ClientAuthMethod.CLIENT_SECRET_POST)
                .build(),
                "backchannel_logout_uri", "dpop_bound_access_tokens", "dpop_require_nonce",
                "grant_types", "jwks", "jwks_uri", "name", "post_logout_redirect_uris",
                "profile", "redirect_uris", "require_par", "scopes", "self_signed_tls_client_auth_thumbprints",
                "tls_client_auth_san_dns", "tls_client_auth_san_uri", "tls_client_auth_subject_dn",
                "tls_client_certificate_bound_access_tokens", "token_endpoint_auth_method");
        assertKeys(UpdateOAuth2ClientRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdateOrganizationRequest: each setter sets exactly its own key. */
    @Test
    void updateOrganizationRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateOrganizationRequest.builder()
                .metadata(JSON.createObjectNode()).build(), "metadata");
        assertKeys(UpdateOrganizationRequest.builder()
                .name("example").build(), "name");
        assertKeys(UpdateOrganizationRequest.builder()
                .slug("example").build(), "slug");
        assertKeys(UpdateOrganizationRequest.builder()
                .metadata(JSON.createObjectNode())
                .name("example")
                .slug("example")
                .build(),
                "metadata", "name", "slug");
        assertKeys(UpdateOrganizationRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdatePermissionRequest: each setter sets exactly its own key. */
    @Test
    void updatePermissionRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdatePermissionRequest.builder()
                .action("example").build(), "action");
        assertKeys(UpdatePermissionRequest.builder()
                .description("example").build(), "description");
        assertKeys(UpdatePermissionRequest.builder()
                .action("example")
                .description("example")
                .build(),
                "action", "description");
        assertKeys(UpdatePermissionRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdateReactorRequest: each setter sets exactly its own key. */
    @Test
    void updateReactorRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateReactorRequest.builder()
                .description("example").build(), "description");
        assertKeys(UpdateReactorRequest.builder()
                .enabled(true).build(), "enabled");
        assertKeys(UpdateReactorRequest.builder()
                .events(java.util.List.of()).build(), "events");
        assertKeys(UpdateReactorRequest.builder()
                .failurePolicy(FailurePolicy.FAIL_CLOSED).build(), "failure_policy");
        assertKeys(UpdateReactorRequest.builder()
                .mode(ReactorMode.INTERCEPT).build(), "mode");
        assertKeys(UpdateReactorRequest.builder()
                .name("example").build(), "name");
        assertKeys(UpdateReactorRequest.builder()
                .priority(1).build(), "priority");
        assertKeys(UpdateReactorRequest.builder()
                .timeoutMs(1).build(), "timeout_ms");
        assertKeys(UpdateReactorRequest.builder()
                .description("example")
                .enabled(true)
                .events(java.util.List.of())
                .failurePolicy(FailurePolicy.FAIL_CLOSED)
                .mode(ReactorMode.INTERCEPT)
                .name("example")
                .priority(1)
                .timeoutMs(1)
                .build(),
                "description", "enabled", "events", "failure_policy", "mode",
                "name", "priority", "timeout_ms");
        assertKeys(UpdateReactorRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdateResourceRequest: each setter sets exactly its own key. */
    @Test
    void updateResourceRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateResourceRequest.builder()
                .metadata(JSON.createObjectNode()).build(), "metadata");
        assertKeys(UpdateResourceRequest.builder()
                .name("example").build(), "name");
        assertKeys(UpdateResourceRequest.builder()
                .parentId(EXAMPLE_ID).build(), "parent_id");
        assertKeys(UpdateResourceRequest.builder()
                .resourceType("example").build(), "resource_type");
        assertKeys(UpdateResourceRequest.builder()
                .metadata(JSON.createObjectNode())
                .name("example")
                .parentId(EXAMPLE_ID)
                .resourceType("example")
                .build(),
                "metadata", "name", "parent_id", "resource_type");
        assertKeys(UpdateResourceRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdateRole: each setter sets exactly its own key. */
    @Test
    void updateRoleSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateRole.builder()
                .description("example").build(), "description");
        assertKeys(UpdateRole.builder()
                .isGlobal(true).build(), "is_global");
        assertKeys(UpdateRole.builder()
                .name("example").build(), "name");
        assertKeys(UpdateRole.builder()
                .description("example")
                .isGlobal(true)
                .name("example")
                .build(),
                "description", "is_global", "name");
        assertKeys(UpdateRole.builder().build());
    }

    /** §27.4 rule 5 for UpdateScopeRequest: each setter sets exactly its own key. */
    @Test
    void updateScopeRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateScopeRequest.builder()
                .description("example").build(), "description");
        assertKeys(UpdateScopeRequest.builder()
                .name("example").build(), "name");
        assertKeys(UpdateScopeRequest.builder()
                .description("example")
                .name("example")
                .build(),
                "description", "name");
        assertKeys(UpdateScopeRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdateServiceAccount: each setter sets exactly its own key. */
    @Test
    void updateServiceAccountSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateServiceAccount.builder()
                .description("example").build(), "description");
        assertKeys(UpdateServiceAccount.builder()
                .name("example").build(), "name");
        assertKeys(UpdateServiceAccount.builder()
                .status(UserStatus.ACTIVE).build(), "status");
        assertKeys(UpdateServiceAccount.builder()
                .description("example")
                .name("example")
                .status(UserStatus.ACTIVE)
                .build(),
                "description", "name", "status");
        assertKeys(UpdateServiceAccount.builder().build());
    }

    /** §27.4 rule 5 for UpdateTenant: each setter sets exactly its own key. */
    @Test
    void updateTenantSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateTenant.builder()
                .metadata(JSON.createObjectNode()).build(), "metadata");
        assertKeys(UpdateTenant.builder()
                .name("example").build(), "name");
        assertKeys(UpdateTenant.builder()
                .slug("example").build(), "slug");
        assertKeys(UpdateTenant.builder()
                .status(TenantStatus.ACTIVE).build(), "status");
        assertKeys(UpdateTenant.builder()
                .metadata(JSON.createObjectNode())
                .name("example")
                .slug("example")
                .status(TenantStatus.ACTIVE)
                .build(),
                "metadata", "name", "slug", "status");
        assertKeys(UpdateTenant.builder().build());
    }

    /** §27.4 rule 5 for UpdateUserRequest: each setter sets exactly its own key. */
    @Test
    void updateUserRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateUserRequest.builder()
                .email("example").build(), "email");
        assertKeys(UpdateUserRequest.builder()
                .metadata(JSON.createObjectNode()).build(), "metadata");
        assertKeys(UpdateUserRequest.builder()
                .status(UserStatus.ACTIVE).build(), "status");
        assertKeys(UpdateUserRequest.builder()
                .username("example").build(), "username");
        assertKeys(UpdateUserRequest.builder()
                .email("example")
                .metadata(JSON.createObjectNode())
                .status(UserStatus.ACTIVE)
                .username("example")
                .build(),
                "email", "metadata", "status", "username");
        assertKeys(UpdateUserRequest.builder().build());
    }

    /** §27.4 rule 5 for UpdateWebhookRequest: each setter sets exactly its own key. */
    @Test
    void updateWebhookRequestSendsOnlyWhatWasSet() throws Exception {
        assertKeys(UpdateWebhookRequest.builder()
                .enabled(true).build(), "enabled");
        assertKeys(UpdateWebhookRequest.builder()
                .events(java.util.List.of()).build(), "events");
        assertKeys(UpdateWebhookRequest.builder()
                .retryPolicy(new RetryPolicy(1.0, 1L, 1)).build(), "retry_policy");
        assertKeys(UpdateWebhookRequest.builder()
                .secret(Sensitive.of("example")).build(), "secret");
        assertKeys(UpdateWebhookRequest.builder()
                .url("example").build(), "url");
        assertKeys(UpdateWebhookRequest.builder()
                .enabled(true)
                .events(java.util.List.of())
                .retryPolicy(new RetryPolicy(1.0, 1L, 1))
                .secret(Sensitive.of("example"))
                .url("example")
                .build(),
                "enabled", "events", "retry_policy", "secret", "url");
        assertKeys(UpdateWebhookRequest.builder().build());
    }

    /**
     * §27.9: a regeneration that stopped emitting builders must fail here.
     *
     * <p>The count is asserted rather than left implicit so that dropping a sparse model from the
     * closure is a failure, not a quietly shorter suite.
     */
    @Test
    void everySparseBodyIsCovered() {
        long cases = java.util.Arrays.stream(ManagementSparseBodiesGeneratedTest.class.getDeclaredMethods())
                .filter(m -> m.getName().endsWith("SendsOnlyWhatWasSet")).count();
        assertEquals(18L, cases,
                "one case per sparse body the schema closure declares");
    }

    /**
     * Asserts the encoded body's key set is exactly {&#64;code expected}, in any order.
     *
     * @param body the request body to render
     * @param expected the wire keys it must carry
     * @throws Exception if the encoded body is not readable JSON
     */
    private static void assertKeys(Object body, String... expected) throws Exception {
        JsonNode node = JSON.readTree(ManagementTransport.encodeBody("test", body));
        List<String> actual = new ArrayList<>();
        node.fieldNames().forEachRemaining(actual::add);
        actual.sort(String::compareTo);
        List<String> want = new ArrayList<>(List.of(expected));
        want.sort(String::compareTo);
        assertEquals(want, actual,
                body.getClass().getSimpleName()
                        + ": a sparse body must carry exactly the fields that were set");
    }
}
